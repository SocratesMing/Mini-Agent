# 知识库检索精准度优化指南

## 问题描述

当知识库中只有一个相关文档时，系统却返回了 5 个相关文档，其中 4 个与问题关联度不高。这是因为：

1. **固定返回数量**：系统默认返回 `top_k=5` 条文档，不考虑相似度
2. **缺少阈值过滤**：没有设置相似度阈值，导致低相似度的文档也被返回

## 解决方案

已通过以下优化实现精准检索：

### 1. 新增配置参数

在 `config.yaml` 的 `vector_store` 部分添加两个参数：

```yaml
vector_store:
  # 初始检索数量（建议设为期望返回数量的 2-3 倍）
  search_top_k: 10
  
  # 相似度阈值 (0-1)，越大越精准
  similarity_threshold: 0.5
```

### 2. 参数说明

#### `search_top_k` - 初始检索数量
- **含义**：从向量数据库中初步检索的文档数量
- **推荐值**：10-20（设为期望返回数量的 2-3 倍）
- **作用**：确保有足够的候选文档进行筛选

#### `similarity_threshold` - 相似度阈值
- **含义**：文档与查询的最小相似度要求
- **范围**：0-1 之间
- **推荐配置**：
  - `0.3-0.5`：宽松模式，返回较多文档，适合需要广泛上下文的场景
  - `0.5-0.7`：中等模式，平衡召回率和精准度（**默认值**）
  - `0.7-0.9`：严格模式，只返回高度相关的文档，适合精准问答
  - `0.9+`：极严格，可能返回很少或没有文档

### 3. 工作原理

```
1. 从数据库检索 top_k=10 条最相关的文档
2. 计算每条文档的相似度分数（similarity = 1 - distance）
3. 过滤掉相似度 < threshold 的文档
4. 返回最终结果（可能少于 top_k 条）
```

### 4. 使用建议

#### 场景 1：精准问答（推荐）
```yaml
search_top_k: 10
similarity_threshold: 0.7
```
- 只返回最相关的 1-2 条文档
- 避免无关信息干扰

#### 场景 2：一般查询
```yaml
search_top_k: 10
similarity_threshold: 0.5
```
- 平衡召回率和精准度
- 适合大多数场景

#### 场景 3：广泛探索
```yaml
search_top_k: 20
similarity_threshold: 0.3
```
- 返回更多相关文档
- 适合需要全面了解的场景

### 5. 调试方法

启用知识库检索后，系统会显示详细的检索信息：

```
🌐 知识库检索已开启
📊 向量数据库状态：enabled=True
🔍 正在检索知识库："你的问题..."
⚙️ 检索参数：top_k=10, 相似度阈值=0.7
✅ 知识库检索完成：找到 2 条相关文档
📄 文档 1: example.pdf [相似度：0.85]
   摘要：相关内容...
📄 文档 2: guide.md [相似度：0.72]
   摘要：相关内容...
📝 已将 2 条相关文档加入上下文
```

通过观察相似度分数，可以调整阈值：
- 如果返回太多不相关文档 → **提高阈值**（如 0.7 → 0.8）
- 如果返回太少或没有文档 → **降低阈值**（如 0.7 → 0.6）

### 6. 配置步骤

1. 找到或创建 `config.yaml` 文件
2. 在 `vector_store` 部分添加或修改参数：
   ```yaml
   vector_store:
     enabled: true
     search_top_k: 10
     similarity_threshold: 0.7  # 根据需求调整
   ```
3. 重启 Mini-Agent 服务
4. 测试查询，观察日志中的相似度分数
5. 根据效果微调阈值

### 7. 技术实现

修改的文件：
- `mini_agent/config.py`：添加配置字段
- `mini_agent/web/utils/vector_store.py`：实现阈值过滤
- `mini_agent/web/service/chat_service.py`：显示检索参数和相似度

核心逻辑：
```python
# Chroma 返回的是余弦距离，转换为相似度分数
similarity = 1.0 - distance

# 只保留相似度大于阈值的文档
if similarity >= similarity_threshold:
    search_results.append(result)
```

## 常见问题

### Q1: 为什么设置了 threshold=0.7 还是没有返回文档？
**A**: 可能所有文档的相似度都低于 0.7。尝试：
- 降低阈值（如 0.6）
- 增加 `search_top_k`（如 20）
- 检查文档是否已正确添加到知识库

### Q2: 相似度分数多少算正常？
**A**: 
- 同一文档的不同片段：0.8-0.95
- 相关文档：0.6-0.8
- 边缘相关：0.4-0.6
- 不相关：< 0.4

### Q3: 如何验证配置是否生效？
**A**: 查看日志输出，会显示：
```
⚙️ 检索参数：top_k=10, 相似度阈值=0.7
[向量数据库] 过滤后返回 2 条结果 (threshold=0.7)
```

## 总结

通过合理配置 `search_top_k` 和 `similarity_threshold`，可以实现：
- ✅ 只返回真正相关的文档
- ✅ 避免无关信息干扰模型判断
- ✅ 提高回答质量和精准度
- ✅ 减少 token 消耗

**推荐起始配置**：
```yaml
search_top_k: 10
similarity_threshold: 0.7
```

然后根据实际效果微调阈值即可。
